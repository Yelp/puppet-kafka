# Script for setting up relevant Kafka environment variables on login to host
# This script only works right in bash
if [ -z "$BASH_VERSION" ]; then
    return
fi

export JAVA_HOME="<%= scope.lookupvar('kafka::server::java_home') %>"
export ZOOKEEPER="<%= scope.lookupvar('kafka::server::zookeeper_connect') %>"
